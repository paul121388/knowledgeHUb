# 测试配置文件
server:
  port: 8104
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/knowledgehub
    username: root
    password: 123456
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: r-7xv7loxk2hxkr4tuthpd.redis.rds.aliyuncs.com
    port: 6379
    timeout: 5000
    password: Train1206
  # Elasticsearch 配置
  # todo 需替换配置
  elasticsearch:
    uris: http://localhost:9200
    username: root
    password: 123456